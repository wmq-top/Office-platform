/**
 * @description: 特殊页面适配中英文的字体大小处理函数
 * @param {*} lenC 中文字符串长度
 * @param {*} width 文字展示区的宽度
 * @param {*} height 文字展示区的高度
 * @return {number} 返回适配的文字大小
 */
const fontSizeCalc = (len, width, height, langType) => {
    const space = width * height;
}